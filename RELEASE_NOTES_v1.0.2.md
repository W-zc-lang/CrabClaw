# LocalAIChat v1.0.2

## 更新内容

- **修复对话界面不显示说话人**：此前用户与 AI 的消息外观完全一致、无法区分。现在每条消息都会明确标注「你」或「AI 助手」，并通过头像配色（你=蓝色 / AI=蓝紫渐变）与左右布局区分，对话归属一目了然。
- **下载模型改为按电脑配置智能推荐**：打开「下载模型」后，软件会先扫描你的设备型号、处理器、内存、显卡与显存、系统盘剩余空间，再据此推荐适配的模型并在列表顶部标注「推荐 · 适配你的电脑」；其余模型作为可选列出。低配机推荐 0.5B/1.5B，主流配置推荐 3B，中高配推荐 7B，高配/独显推荐 14B。

## 使用说明

解压 `LocalAIChat.zip`，双击 `LocalAIChat/LocalAIChat.exe` 即可。首次启动若提示下载基础模型，等待进度条完成即可自动创建内置 AI（小墨 / 子成）。

## 验证

- 源码 `py_compile` 与 `node --check` 均通过。
- 对话角色显示、硬件扫描与推荐逻辑已随本版本发布。GUI 实跑请在 Windows 本机验证。
