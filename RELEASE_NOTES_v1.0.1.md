# LocalAIChat v1.0.1

本地 AI 对话桌面应用，数据不出本机，内置「小墨」（通用助手）与「子成」（专属 AI）。

## 本次更新

- 修复窗口「未响应」问题：将 Ollama 启动、文档摘要生成、文件操作三类耗时任务从主线程移至后台线程，界面在模型准备与推理期间保持可操作。
- 优化启动流程：Ollama 状态探测改为后台刷新并通过事件推送，不再阻塞界面加载。

## 功能一览

- **本地多模型**：内置小墨、子成，首次启动自动准备。
- **电脑文件控制**：设置中可开关；开启后 AI 可列出目录、读取文件、移动/重命名/删除文件、执行白名单命令，写操作需二次确认并记录日志。
- **模型下载进度条**：首次约 2GB 基础模型下载显示实时进度。
- **白色主题 UI**：简洁明亮风格，支持 Bionic Reading 文本加粗。
- **办公能力**：本地知识库、表格分析（CSV/XLSX）、文件整理与批量重命名。
- **多会话历史**：自动命名、重新生成、停止生成。

## 使用方式

1. 下载 `LocalAIChat.zip` 并解压。
2. 双击 `LocalAIChat/LocalAIChat.exe`。
3. 首次启动自动下载约 2GB 的 qwen2.5:3b 基础模型并创建内置 AI，请保持网络畅通。
4. 之后完全离线可用。
